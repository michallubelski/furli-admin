# Copy this file to .htpasswd (gitignored) and replace with a real bcrypt hash before deploying.
# Generate with: htpasswd -nB <username>
# Example (placeholder only - do not use in production):
# admin:$2y$05$replace.this.with.a.real.bcrypt.hash.generated.locally
